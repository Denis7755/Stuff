## Compare encoding ROM-I with ROM-II. Was ROM-II significantly more difficult, or were they about the same?
# They were about the same as I did the manual calculations back in ROM I, and when I saw that ROM II is the same in structure (just with jmp/comparisons), I was able to approach it in the same way. 

## How was using the built-in ROM vs making your own?
# Personally, I enjoyed making my own ROM, then using the built-in. Becuase for my own, I figure out the constents, wire them up to the mux, and get the output of a 8 bit. But for built-in ROM, I found myself figuring out what went wrong, until i figured the address bid width needed to be 3 instead of 4. Because 3 bandwidth for an 8-bit (not 4 as that one is for 16 bits)
